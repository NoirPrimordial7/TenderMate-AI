/*
Future API integration placeholder.

Target flow:
Frontend
  -> TenderService
  -> TenderRepository
  -> API client
  -> FastAPI
  -> AI Analysis

Keep API calls behind repository implementations so UI components and pages do not change
when JSON data is replaced by FastAPI, Supabase, or AI-generated tender analyses.
*/

export {};
